# Copy this to .env for local dev, or set as an environment variable
# in your Vercel project settings for production.

# Local development (backend running on your machine):
VITE_API_BASE_URL=http://localhost:10000

# Production (after backend is deployed on Render), set this instead:
# VITE_API_BASE_URL=https://your-backend.onrender.com